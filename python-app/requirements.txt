# Python Test App Dependencies
# Contains intentionally vulnerable packages for testing

flask==2.3.0
pypdf==3.1.0          # CVE-2022-42969 - ReDoS vulnerability
requests==2.28.0
boto3==1.26.0
py4j==0.10.9.5        # Transitive Log4j dependency (for unreachable test)

# Dev dependencies (should not appear in production scan)
pytest==7.0.0

# Public packages (known vulnerable — CVE test targets)
requests==2.31.0
flask==2.0.3

# Private packages from devpi (must match what setup.sh publishes)
authlib==1.3.0
jinja2==3.1.2
internal-sdk==0.5.0
